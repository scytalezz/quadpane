# Quadpane 인터페이스 설계

Q-Dir처럼 여러 폴더를 동시에 보면서, 경로 이동과 패널 사이의 파일 작업을 한 화면에서 처리한다. 현재 `0.4.0` 소스를 기준으로 정리했다.

소스 기준 갱신: 2026-09-14

## 화면 원칙

- 각 패널은 편집 가능한 전체 경로와 파일명·수정일·크기 목록을 제공한다.
- 검색·새로고침·복사·잘라내기·붙여넣기·다른 창으로·파일 정보를 공통 영역에 모은다.
- 활성 패널과 선택 항목은 파란색으로 구분하고, 키보드 포커스는 테두리로 표시한다.
- 파일 목록은 좁은 패널에서도 글자 크기와 날짜 열을 유지하며 가로 스크롤한다.
- 로딩·빈 폴더·검색 결과 없음·접근 오류를 구분하고 재시도를 제공한다.
- 파일 작업 결과는 완료·건너뜀·실패로 구분하고, 문제 항목의 경로와 이유를 남긴다.
- 작업 상태 줄은 미리 공간을 확보하고 상세 결과·메뉴는 겹쳐 표시해 작업 중 창이나 패널이 늘어나지 않게 한다.
- 파일·폴더와 빈 공간에 각기 맞는 기본 메뉴를 제공한다. Windows 셸 확장 메뉴는 포함하지 않는다.
- 일반 브라우저에서는 포터블 앱 실행 안내를 표시한다.

```text
+------------+-------------------------------------------+
| Quadpane   | 열린 폴더 검색                  테마 / 도움말 |
+------------+-------------------------------------------+
| 작업 공간   | 작업 공간 이름                   화면 분할 |
|            | 새로고침 / 복사·이동 / 파일 정보            |
|            | 복사 대기·진행 / 저장 상태       작업 결과 |
| 즐겨찾기 ＋ +---------------------+---------------------+
|            | 전체 경로 입력 / 목록 | 전체 경로 입력 / 목록 |
|            | 파일명 / 날짜 / 크기  | 파일명 / 날짜 / 크기  |
|            +---------------------+---------------------+
| 내 드라이브 | 전체 경로 입력 / 목록 | 전체 경로 입력 / 목록 |
|            | 파일명 / 날짜 / 크기  | 파일명 / 날짜 / 크기  |
+------------+---------------------+---------------------+
| PC 연결    | 현재 경로 / 선택 정보                     |
+------------+-------------------------------------------+
```

## 탐색·선택·복원

주소창 펼침 목록은 현재 위치·상위 폴더, 즐겨찾기, 최근 경로, 드라이브를 묶어서 표시하고 중복 경로를 제거한다. 주소창의 `Alt+↓`로 열고 방향키·`Enter`로 선택한다. 즐겨찾기의 `＋`는 현재 폴더를 추가하며, 직접 추가한 항목에는 제거 버튼을 제공한다.

각 패널은 경로·선택·정렬·뒤로 이력과 주소 편집 상태를 독립적으로 관리한다. `Ctrl` 선택과 `Shift` 범위 선택, `Ctrl+A` 전체 선택을 지원한다. 복사·이동 대기 목록은 작업 공간을 바꿔도 유지한다. **다른 창으로** 대화상자는 다른 작업 공간을 포함한 열린 폴더를 대상으로 제시한다.

두 작업 공간의 패널 경로·정렬·분할·활성 패널은 `quadpane-data/session.json`에 저장한다. 사용자 즐겨찾기·최근 경로는 `preferences.json`, 테마는 브라우저 저장소에 보관한다. 검색어·항목 선택·뒤로 이력·복사 대기는 실행 중 상태다.

성공한 폴더 조회만 새 위치로 적용한다. 실패한 경로는 오류와 함께 남기고, 뒤늦은 응답이 최신 탐색이나 편집 중 주소를 덮어쓰지 않도록 요청을 구분한다. 파일 작업이 끝나면 관련 패널을 갱신한다.

## 기본 메뉴·파일 관리·드래그

파일 행의 오른쪽 클릭은 기존 여러 항목 선택을 유지한다. 선택하지 않은 행을 누르면 해당 행을 선택하고, 빈 공간을 누르면 선택을 해제하고 폴더 작업 메뉴를 연다. `Shift+F10`과 메뉴 키로도 열 수 있으며 방향키·`Home`·`End`로 이동하고 `Escape`·`Tab`으로 닫아 목록에 포커스를 돌려준다. 사용할 수 없는 명령은 비활성화한다.

| 대상 | 메뉴 |
| --- | --- |
| 파일·폴더 항목 | 열기, 잘라내기, 복사, 붙여넣기, 다른 창으로, 이름 변경, 삭제, 탐색기에서 보기, 파일 정보 |
| 목록 빈 공간 | 새 폴더, 붙여넣기, 새로고침, 모두 선택, 탐색기에서 보기 |

두 번 클릭과 `Enter`는 폴더로 이동하거나 파일을 기본 앱으로 연다. 파일 정보는 툴바·메뉴·`Alt+Enter`로 제공한다. 이름 변경은 `F2`, 새 폴더는 `Ctrl+Shift+N`으로 입력 창을 열며 충돌과 잘못된 이름은 입력 창 안에 표시한다. 이름 변경이 끝나면 해당 경로를 쓰는 패널·즐겨찾기·최근 경로·이력·복사 대기 목록을 갱신한다.

`Delete`는 선택 항목을 보여주는 확인 창을 열고 **취소**에 처음 포커스를 둔다. 확인을 눌렀을 때만 휴지통 이동을 요청한다. 취소·닫기·`Escape`는 요청하지 않는다. 영구 삭제는 제공하지 않으며 휴지통 실패는 결과로 알린다. 시작한 파일 변경 작업은 하나씩 완료하고, 진행 중 창 닫기는 완료 후 종료한다.

앱 내부 드래그는 실행 중인 선택과 연결된 토큰을 사용한다. 다른 패널의 목록이나 폴더 행이 대상이며, 대상 강조와 복사·이동 문구는 겹쳐 표시한다. 같은 드라이브·UNC 공유 루트는 이동, 다른 루트는 복사를 기본으로 하고 `Ctrl`·`Shift`로 각각 복사·이동을 지정한다. 문자열로 넘긴 외부 경로는 받지 않고 preload에서 실제 `File` 객체의 경로를 추출한다. `Alt`를 누른 채 시작하면 네이티브 파일 드래그를 요청하고 앱에 포커스가 돌아올 때 패널을 새로고침한다.

복사 대기는 앱 내부 상태로 Windows 클립보드와 공유하지 않는다. 외부 드래그의 운영체제·대상 앱 동작 전체는 자동 검증 범위에 포함하지 않으며, 실제 외부 마우스 드롭 완료도 아직 수동 검증하지 않았다.

## 화면 크기와 작업 상태

[styles.css](styles.css)의 앱 프레임은 `100dvh`이며 문서 전체 스크롤을 막는다. 주 영역은 머리글·툴바·작업 상태·패널·상태 표시줄로 나눈 그리드이고, 패널은 `minmax(0, 1fr)` 공간에 들어간다. 파일 목록과 긴 툴바에는 자체 스크롤을 제공한다. 창은 사용자가 크기를 조절할 수 있으며 파일 작업이 창 크기를 자동 변경하지 않는다.

툴바 아래 작업 상태 줄은 기본 30px를 항상 확보한다. 화면 높이가 680px 이하이면 28px로 줄이고, 너비 540px 이하에서는 30px를 사용한다. 복사 대기·작업 중·설정 저장 오류가 같은 줄을 사용하며 긴 문구는 생략 표시한다. **작업 결과** 버튼의 팝오버는 완료·건너뜀·실패와 경로를 표시하고 별도로 스크롤한다. 메뉴·경로 목록·드롭 안내·결과는 패널 배치에 공간을 추가하지 않으며 화면 가장자리 안에 위치시킨다.

## 시각 체계

| 역할 | 라이트 | 다크 |
| --- | --- | --- |
| 앱 배경 | `#F4F6F8` | `#151719` |
| 파일 패널 | `#FFFFFF` | `#1E2126` |
| 기본 글자 | `#202938` | `#F0F2F5` |
| 보조 글자 | `#606D80` | `#B9C2CE` |

중성 계열 다크 배경에 글자·구분선·선택 영역의 대비를 높였다. 파일 행은 28px, 파일명은 13px, 날짜·크기는 12px, 파일 아이콘은 18px로 유지한다. 아이콘은 작은 글자 대신 파일 종류별 도형을 사용한다.

글꼴은 Segoe UI Variable, Segoe UI, 맑은 고딕 순이다. 파일명·경로는 왼쪽, 크기는 오른쪽 정렬한다. 대화상자·경로 목록·입력란·스크롤바·파일 아이콘은 [styles.css](styles.css)의 테마 토큰을 공유한다. 초기 테마는 시스템 설정을 따르고 사용자 선택은 저장한다.

## 구현 경계

[app.js](app.js)가 UI와 작업 상태를 관리하고 [desktop/preload.cjs](desktop/preload.cjs)를 통해 [desktop/main.cjs](desktop/main.cjs)에 요청한다. [desktop/filesystem.cjs](desktop/filesystem.cjs)는 디렉터리 메타데이터를 조회하고, [desktop/file-operations.cjs](desktop/file-operations.cjs)는 경로 검증과 복사·이동을 처리한다. [desktop/explorer-actions.cjs](desktop/explorer-actions.cjs)는 기본 앱 열기·탐색기 표시·휴지통·이름 변경·새 폴더와 네이티브 드래그 경로 검증을 맡는다. 메인 프로세스의 공통 잠금으로 파일 변경 작업의 중복 실행을 막는다. [desktop/session-store.cjs](desktop/session-store.cjs)와 [desktop/preferences-store.cjs](desktop/preferences-store.cjs)는 각각 작업 공간과 즐겨찾기·최근 경로를 저장한다.

복사·이동은 기존 대상의 덮어쓰기·병합을 허용하지 않으며 링크·정션을 거부한다. 이름 변경과 새 폴더도 기존 항목을 덮어쓰지 않는다. 휴지통 동작은 Electron `shell.trashItem`을 사용하며 영구 삭제로 대체하는 처리가 없다. 실행 방법은 [PORTABLE.md](PORTABLE.md), 실제 실행 검증 범위와 한계는 [VERIFICATION.md](VERIFICATION.md)에 정리했다.
