# pane 0.4.0 기본 파일 작업·고정 레이아웃 검증

> 이 문서는 프로젝트 이름이 **Quadpane**으로 바뀌기 전(구 이름 `pane`) 0.4.0 빌드의 검증 기록이다. 본문 파일명·경로·해시는 당시 산출물 기준으로 보존한다. 이전 기록이며, 2026-09-23 셸 메뉴/OLE 변경에는 적용되지 않는다. 새 검증은 [셸 통합 문서](docs/windows-shell.md)를 참고한다.

검증일: 2026-09-14. Windows x64 최종 포터블 앱과 동일 UI 소스를 검사했다. 아래 실행 보고서는 최종 소스·패키지를 대상으로 한다.

## 결과

| 검사 | 결과 |
| --- | --- |
| JavaScript 문법 검사 | `npm run check` 종료 코드 0 |
| 단위 테스트 | 37/37 통과, 건너뜀 없음, 실제 D:→C: 이동 포함 |
| 브라우저 UI·실제 마우스·다크 가독성 | 8개 검사 그룹 통과, 런타임 오류 없음 |
| 실제 앱의 우클릭·드래그·휴지통·창 크기 | 7개 검사 그룹 통과, `mouseMode: true`, 런타임 오류 없음 |
| 즐겨찾기·작업 공간 간 파일 작업·작업 중 종료 | 6개 검사 그룹 통과 |
| 실제 탐색·주소 입력·재실행 복원 | 5개 검사 그룹 통과 |
| 최종 포터블 빌드 | 종료 코드 0 |
| 포터블 EXE 첫 실행·재실행·설정 이동 | 3회 모두 종료 코드 0 |
| ASAR 파일 목록·소스 바이트 비교 | 13개 허용 파일만 포함, 현재 소스와 일치 |
| 독립 검증 / 반례 검토 | PASS / APPROVE |

Typecheck와 lint는 별도 구성이 없다. Git 저장소가 없어 파일 해시로 검증 대상을 식별한다.

## 실제 기본 파일 작업

`scripts/verification/verify-explorer.cjs --mouse-drag`는 패키징된 앱을 숨김 실행하고 테스트가 생성한 파일·폴더만 변경했다.

- 선택된 파일에서 우클릭해도 다중 선택 유지, 메뉴 복사와 대상 빈 공간의 붙여넣기로 실제 두 파일 생성
- 이름 충돌 시 두 원본 보존 및 대화상자 유지, `F2`로 한글 이름 변경, `Ctrl+Shift+N`으로 실제 새 폴더 생성
- 실제 마우스로 같은 드라이브의 다른 패널에 이동, `Ctrl` 드래그로 복사, `Shift` 드래그로 폴더 행 내부에 이동
- 디스크에 연결된 실제 `File` 객체의 경로를 preload에서 추출하고 외부 드롭 경로로 복사
- 파일 더블클릭·탐색기 표시·Alt 드래그가 올바른 경로로 Electron API에 도달함을 확인
- Delete 후 취소하면 파일 유지, 삭제 확인 후 실제 `shell.trashItem`으로 테스트 파일만 휴지통으로 이동

기본 앱 열기·탐색기 표시·외부로 드래그하는 OS 호출은 검사에서 가로챘다. 실제 외부 프로그램 실행이나 다른 앱으로의 드롭 완료를 검증한 것으로 간주하지 않는다. 입력 드롭도 실제 디스크 `File`을 테스트에서 전달했으며, Windows 탐색기에서 마우스로 가져오는 전체 과정과 구별한다. 앱 내부 패널 드래그는 실제 마우스 입력과 실제 파일 변경을 모두 확인했다.

백엔드 테스트는 휴지통 사용 불가 시 원본 보존, 상위·하위 항목 중복 선택, 이름 충돌·대소문자 변경·실패 복구, 정션 차단, 변경 중 중복 실행 잠금을 확인했다. 실제 NTFS에 `file.txt`와 `file.txt `를 각각 만들고 뒤 공백이 있는 이름을 선택했을 때 일반 이름의 파일을 대신 변경하지 않는 회귀 검사도 통과했다.

복사·이동 회귀 검사는 한글 경로, 중첩·빈 폴더, 크기·수정 시각, NTFS 추가 스트림 보존, 충돌·대상 경합·중간 실패 시 원본과 기존 대상 보존, 실제 D:→C: 파일·폴더 이동을 포함한다. 실제 앱에서 32 MiB 파일 복사 도중 종료를 요청하고, 완료 뒤 종료 및 원본·대상 SHA256 일치를 확인했다.

## 화면 크기와 입력

네이티브 앱의 유휴·복사 준비·시작·완료·결과 팝업·이름 변경/폴더 생성·드래그 완료·휴지통 완료 8단계에서 다음 값이 모두 동일했다.

- Windows 창 좌표와 크기: `(1000, 216, 1440, 960)`
- 렌더러 뷰포트와 문서 크기: `1424 × 921`
- 표시된 네 패널의 좌표와 너비·높이

브라우저 760×540에서도 메뉴, 복사 준비·진행·완료, 결과 팝업 전후 문서와 패널 크기가 동일했다. 작업 상태는 고정 높이 영역에, 상세 결과는 겹쳐지는 팝오버에 표시한다.

브라우저 검사는 라이트·다크 저장, 28px 행 높이, 파일명·날짜의 일반 배경 대비 7:1 이상, 날짜·크기 정렬, 1024·760·390px에서 문서 가로·세로 넘침 없음도 확인했다. 비동기 탐색의 오래된 응답 무시, 입력 초안 유지, 실패 경로 미저장, 완료 후 새로고침과 진행 중 탐색 간 경합, 부분 이동 재시도도 통과했다.

초기 실제 Shift 드래그 검사는 실패했다. 이벤트 추적으로 폴더 드롭 이전에 파일 행의 자식 요소에서 드래그 시작이 차단됨을 확인했고, 해당 요소가 행의 `-webkit-user-drag` 값을 상속하도록 수정했다. 테스트 전용 CSS 주입을 제거한 최종 소스에서 파일명·날짜·크기 각각을 잡는 실제 Shift 드래그 및 Shift 클릭 범위 선택을 재검증했다. 최종 네이티브 앱에서도 실제 Shift 폴더 드롭을 통과했다.

화면 캡처는 브라우저에서 저장하고 네이티브 실행 상태는 접근성 스냅샷과 좌표 기록으로 남겼다.

## 최종 실행 기록

| 대상 | 보고서 |
| --- | --- |
| 브라우저 UI·마우스 | `.checks/browser-files/run-1789366693543/result.json` |
| 실제 기본 파일 작업·마우스·고정 크기 | `.checks/explorer/run-1789366728581/result.json` |
| 실제 즐겨찾기·복사·이동·종료 | `.checks/workflows/run-1789366787320/result.json` |
| 실제 탐색·재실행 | `.checks/real-files/run-1789366868688/result.json` |
| 실제 포터블 런처 | `.checks/portable-launch/run-1789366842009/result.json` |

세 네이티브 보고서의 ASAR 해시가 아래 최종 패키지와 일치한다. 포터블 런처 보고서는 EXE 해시와 첫 실행·재실행·EXE 및 `pane-data/`를 옮긴 후 실행을 기록한다. 런처 검사에서 `PORTABLE_EXECUTABLE_DIR`을 주입하지 않았으며, 세 실행 모두 경로·2분할·수정 날짜·다크 모드·즐겨찾기·실제 드라이브 및 정상 종료를 확인했다.

독립 검증자는 문법 검사와 37개 테스트를 직접 재실행하고 실제 ASAR 파일 목록·현재 소스 바이트·EXE 해시를 비교했다. 별도의 반례 검토자는 격리된 복사본에서 백엔드 테스트와 IPC 발신자·프레임·URL 차단, 공통 잠금·종료 대기, 드래그 토큰·수정 키·키보드 메뉴를 실행 검증했다. 모든 실제 앱/UI 보고서는 주 작업자가 실행했으며 두 검토자가 검사 코드와 기록을 독립적으로 확인했다.

## 산출물과 범위

파일: `dist-portable/pane-0.4.0-portable-x64.exe`

크기: 109,246,168 bytes (약 109 MB / 104.19 MiB)

```text
EXE  00031C54703A98C51CE45503058C5D1D3C287BF9E8FFBDDAEA6EDAC4E0A127ED
ASAR 87D6567D34A402628B6A90942FF0EC99A28EC6EC32549E5FF8563C2D3DBA3274
```

우클릭 메뉴는 앱 자체의 기본 파일 메뉴이며 Windows 셸 확장 메뉴를 포함하지 않는다. 복사·잘라내기 클립보드는 앱 내부에서 공유한다. 기존 항목 덮어쓰기·폴더 병합·영구 삭제·전송 취소·배포용 코드 서명은 지원하지 않는다. 링크·정션을 포함한 변경 작업과 실제 네트워크 공유·외장 드라이브 분리 상황, 외부 앱 드롭 완료는 실기 검증 범위에 포함하지 않는다.
